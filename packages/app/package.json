{
	"name": "@chiel/monorepo-ci-app",
	"version": "0.1.0",
	"repository": {
		"url": "github:chiel/monorepo-ci",
		"directory": "packages/app"
	},
	"scripts": {
		"build": "mkdir dist && touch dist/boop"
	},
	"jobs": [
		"docker-build"
	]
}
